AIM:Develop a program in c to implement function that takes a pointer to ana array and its size,and returns the average of its elements.
program:
 
      #include <stdio.h>

float findAverage(int *arr, int size) {
    int sum = 0;
    for(int i = 0; i < size; i++) {
        sum += *(arr + i);  
    }
    return (float)sum / size;
}

int main() {
    int arr[50], n;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter %d elements:\n", n);
    for(int i = 0; i < n; i++)
        scanf("%d", &arr[i]);

    float avg = findAverage(arr, n);

    printf("\nAverage of array elements = %.2f\n", avg);

    return 0;
}
