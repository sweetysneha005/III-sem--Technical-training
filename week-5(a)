AIM:Develeop a program in c to implement merge two arrays.
program:
        #include <stdio.h>

int main() {
    int a[50], b[50], c[100];
    int n1, n2, i, j, k = 0;

    printf("Enter number of elements in first array: ");
    scanf("%d", &n1);
    printf("Enter elements of first array:\n");
    for(i = 0; i < n1; i++)
        scanf("%d", &a[i]);

    printf("Enter number of elements in second array: ");
    scanf("%d", &n2);
    printf("Enter elements of second array:\n");
    for(i = 0; i < n2; i++)
        scanf("%d", &b[i]);

    
    for(i = 0; i < n1; i++)
        c[k++] = a[i];
    for(j = 0; j < n2; j++)
        c[k++] = b[j];

    printf("\nMerged Array:\n");
    for(i = 0; i < k; i++)
        printf("%d ", c[i]);

    printf("\n");
    return 0;
}
