AIM:Develop a program in c to implement merge contents of two files into a third file.
program:
        #include <stdio.h>
#include <stdlib.h>

int main() {
    FILE *file1, *file2, *file3;
    char ch;

    // Open first file in read mode
    file1 = fopen("file1.txt", "r");
    if(file1 == NULL) {
        printf("Unable to open file1.txt\n");
        exit(1);
    }

    // Open second file in read mode
    file2 = fopen("file2.txt", "r");
    if(file2 == NULL) {
        printf("Unable to open file2.txt\n");
        fclose(file1);
        exit(1);
    }

    // Open third file in write mode
    file3 = fopen("merged.txt", "w");
    if(file3 == NULL) {
        printf("Unable to create merged.txt\n");
        fclose(file1);
        fclose(file2);
        exit(1);
    }

    // Copy contents of first file to third file
    while((ch = fgetc(file1)) != EOF) {
        fputc(ch, file3);
    }

    // Copy contents of second file to third file
    while((ch = fgetc(file2)) != EOF) {
        fputc(ch, file3);
    }

    printf("Files merged successfully into merged.txt\n");

    fclose(file1);
    fclose(file2);
    fclose(file3);

    return 0;
}
